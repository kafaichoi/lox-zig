// This test demonstrates that closures properly capture variables
// from their lexical scope even when a new variable with the same name
// is declared after the function is defined but before it's called.

var a = "global";

{
  fun showA() {
    print a;
  }

  showA();  // Should print "global"
  var a = "block";
  showA();  // Should still print "global" with proper lexical scoping
} 